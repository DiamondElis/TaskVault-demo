# T151 — real IRSA role ARNs substituted at deploy time from CDK outputs.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: backend-sa
  namespace: demo-prod
  annotations:
    eks.amazonaws.com/role-arn: __TASKVAULT_BACKEND_ROLE_ARN__
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: worker-sa
  namespace: demo-prod
  annotations:
    eks.amazonaws.com/role-arn: __TASKVAULT_WORKER_ROLE_ARN__
---
# T157 — db-migrator reads RDS creds from Secrets Manager via backend IRSA role.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: db-migrator-sa
  namespace: demo-prod
  annotations:
    eks.amazonaws.com/role-arn: __TASKVAULT_BACKEND_ROLE_ARN__
